---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: nificonnections.nifi.konpyutaika.com
spec:
  group: nifi.konpyutaika.com
  names:
    kind: NifiConnection
    listKind: NifiConnectionList
    plural: nificonnections
    singular: nificonnection
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NifiConnection is the Schema for the nificonnections API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NifiConnectionSpec defines the desired state of NifiConnection
            properties:
              configuration:
                description: the Configuration of the connection
                properties:
                  backPressureDataSizeThreshold:
                    description: The maximum data size of objects that can be queued
                      before back pressure is applied
                    type: string
                  backPressureObjectThreshold:
                    description: The maximum number of objects that can be queued
                      before back pressure is applied
                    format: int64
                    type: integer
                  bends:
                    description: The bend points on the connection
                    items:
                      properties:
                        posX:
                          description: The x coordinate.
                          format: int64
                          type: integer
                        posY:
                          description: The y coordinate.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  flowFileExpiration:
                    description: The maximum amount of time an object may be in the
                      flow before it will be automatically aged out of the flow
                    type: string
                  loadBalanceCompression:
                    description: Whether or not data should be compressed when being
                      transferred between nodes in the cluster
                    type: string
                  loadBalancePartitionAttribute:
                    description: The FlowFile Attribute to use for determining which
                      node a FlowFile will go to
                    type: string
                  loadBalanceStrategy:
                    description: How to load balance the data in this Connection across
                      the nodes in the cluster
                    type: string
                  prioritizers:
                    description: The comparators used to prioritize the queue
                    items:
                      type: string
                    type: array
                type: object
              destination:
                description: the Destination component of the connection.
                properties:
                  name:
                    description: the name of the component.
                    type: string
                  namespace:
                    description: the namespace of the component.
                    type: string
                  subName:
                    description: the name of the sub component (e.g. queue or port
                      name)
                    type: string
                  type:
                    description: the type of the component (e.g. nifidataflow).
                    type: string
                required:
                - name
                - type
                type: object
              source:
                description: the Source component of the connection.
                properties:
                  name:
                    description: the name of the component.
                    type: string
                  namespace:
                    description: the namespace of the component.
                    type: string
                  subName:
                    description: the name of the sub component (e.g. queue or port
                      name)
                    type: string
                  type:
                    description: the type of the component (e.g. nifidataflow).
                    type: string
                required:
                - name
                - type
                type: object
            required:
            - destination
            - source
            type: object
          status:
            description: NifiConnectionStatus defines the observed state of NifiConnection
            properties:
              connectionID:
                description: connection ID
                type: string
              state:
                description: connection current state.
                type: string
            required:
            - connectionID
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
